runtime: java
env: flex

runtime_config:  # Optional
  jdk: openjdk8

handlers:
- url: /.*
  script: this field is required, but ignored

manual_scaling:
  instances: 1

env_variables:
  CLOUD_SQL_CONNECTION_NAME: "travelrobot:australia-southeast1:travelrobot"
  DB_NAME: "travelrobot"
  DB_USER: "root"
  DB_PASS: "UlabTravel1234"

resources:
  cpu: 1
  memory_gb: 2
  disk_size_gb: 10